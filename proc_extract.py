import sys

proc_name = 'extract'
proc_title = 'Extract Indices'

def set(parent):
    return

def check_all(source='input'):
    return {},{}

def run():
    sys.stderr.write('Running process {}.\n'.format(proc_name))
