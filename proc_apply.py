import sys

proc_name = 'apply'
proc_title = 'Apply Formula'

def set(parent):
    return

def check(source='input'):
    return {},{}

def run():
    sys.stderr.write('Running process {}.\n'.format(proc_name))
