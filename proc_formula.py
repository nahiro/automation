import sys

proc_name = 'formula'
proc_title = 'Make Formula'

def set(parent):
    return

def check(source='input'):
    return {},{}

def run():
    sys.stderr.write('Running process {}.\n'.format(proc_name))
